#Linear regresion script with only numpy
import numpy as np

#import matplotlib to make the graphic
import matplotlib,pyplot as plt


def estimate_b_0_b_1:
    n = np.size(x)
    m_x, m_y = np.mean(x), np,mean(y)
    

